
//  gives a random velocity vector
var randomVector = function(min,max) {
    var newVector = new PVector (    
        random(-min, max),
        random(-min, -max)
        );
    return newVel;
};
    
